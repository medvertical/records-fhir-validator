# Tests — runtime engine doesn't need them in the published tarball
**/__tests__/
**/*.test.ts
**/*.spec.ts

# TS build artefacts (we ship .ts source today; once tsc build lands,
# this list becomes ./src/ and ./dist/ both shipped)
*.tsbuildinfo

# Editor / OS
.DS_Store
.vscode/
.idea/
